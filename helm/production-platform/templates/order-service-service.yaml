apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: order-service

  ports:
    - protocol: TCP
      port: {{ .Values.orderService.servicePort }}
      targetPort: {{ .Values.orderService.servicePort }}

  type: {{ .Values.orderService.serviceType }}
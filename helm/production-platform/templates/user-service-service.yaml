apiVersion: v1
kind: Service

metadata:
  name: user-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: user-service

  ports:
    - port: {{ .Values.userService.servicePort }}
      targetPort: {{ .Values.userService.servicePort }}

  type: {{ .Values.userService.serviceType }}